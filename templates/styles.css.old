:root {
  --bg-color: #f3f4f6;
  --card-bg: #ffffff;
  --text-color: #1f2937;
  --text-secondary: #6b7280;
  --accent-color: #2563eb;
  --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

[data-theme="dark"] {
  --bg-color: #1f2937;
  --card-bg: #374151;
  --text-color: #f9fafb;
  --text-secondary: #9ca3af;
  --accent-color: #60a5fa;
  --shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background-color: var(--bg-color);
  color: var(--text-color);
  transition: background-color 0.3s, color 0.3s;
  min-height: 100vh;
}

nav {
  background-color: var(--card-bg);
  box-shadow: var(--shadow);
  padding: 1rem 2rem;
}

.nav-container {
  max-width: 1024px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nav-container h1 {
  margin: 0;
  font-size: 1.5rem;
  font-weight: 700;
}

.nav-links {
  display: flex;
  gap: 1rem;
  align-items: center;
}

.nav-links a {
  color: var(--text-secondary);
  text-decoration: none;
  font-weight: 500;
}

.nav-links a:hover,
.nav-links a.active {
  color: var(--accent-color);
}

.theme-toggle {
  background: none;
  border: none;
  cursor: pointer;
  padding: 0.5rem;
  border-radius: 50%;
  background-color: var(--text-secondary);
  display: flex;
  align-items: center;
  justify-content: center;
}

.theme-toggle svg {
  width: 1.25rem;
  height: 1.25rem;
  fill: var(--card-bg);
}

.container {
  max-width: 1024px;
  margin: 2rem auto;
  padding: 0 1rem;
}

.admin-panel, .form-container, .manage-container {
  background-color: var(--card-bg);
  padding: 2rem;
  border-radius: 0.5rem;
  box-shadow: var(--shadow);
}

.admin-panel h2, .form-container h2, .manage-container h2 {
  margin: 0 0 1.5rem;
  font-size: 1.5rem;
  font-weight: 600;
  text-align: center;
}

.admin-options {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
}

.admin-option {
  background-color: var(--bg-color);
  padding: 1rem;
  border-radius: 0.25rem;
  text-align: center;
}

.admin-option a {
  color: var(--accent-color);
  text-decoration: none;
  font-weight: 500;
  font-size: 1rem;
}

.admin-option a:hover {
  text-decoration: underline;
}

.form-container label, .manage-container label {
  display: block;
  font-size: 0.875rem;
  font-weight: 500;
  margin-bottom: 0.25rem;
  color: var(--text-color);
}

.form-container input, .form-container textarea, .manage-container input, .manage-container textarea {
  width: 100%;
  padding: 0.5rem;
  border: 1px solid var(--text-secondary);
  border-radius: 0.25rem;
  background-color: var(--card-bg);
  color: var(--text-color);
  margin-bottom: 1rem;
  font-size: 1rem;
}

.form-container textarea {
  resize: vertical;
  min-height: 200px;
}

.form-container button, .manage-container button {
  background-color: var(--accent-color);
  color: var(--card-bg);
  padding: 0.5rem 1rem;
  border: none;
  border-radius: 0.25rem;
  cursor: pointer;
  font-weight: 500;
  width: 100%;
  font-size: 1rem;
}

.form-container button:hover, .manage-container button:hover {
  background-color: #1d4ed8;
}

.post-list {
  display: grid;
  gap: 1rem;
}

.post-item {
  background-color: var(--bg-color);
  padding: 1rem;
  border-radius: 0.25rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.post-item p {
  margin: 0;
  font-size: 1rem;
}

.post-item .actions {
  display: flex;
  gap: 0.5rem;
}

.post-item button {
  padding: 0.25rem 0.75rem;
  font-size: 0.875rem;
  width: auto;
}